- squash and push
- better join message
  
  Thanks for inviting me! Install the <bumps gem> and drop this into *env.rb* to get rolling:
  
  require 'bumps'
  
  Bumps.configure do
    pull_from 'http://localhost:1981'
    push_to 'url'
  end
  
- push and post feature names to wave
- push and add summary to first blip
- push and highlight steps